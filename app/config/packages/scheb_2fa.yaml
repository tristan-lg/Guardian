# See the configuration reference at https://symfony.com/bundles/SchebTwoFactorBundle/6.x/configuration.html
scheb_two_factor:
    security_tokens:
        - Symfony\Component\Security\Core\Authentication\Token\UsernamePasswordToken
        - Symfony\Component\Security\Http\Authenticator\Token\PostAuthenticationToken

    # TOTP authentication config
    totp:
        enabled: true
        server_name: '%app.name%'
        issuer: '%app.name%'
        leeway: 5
        template: '@pages/security/two-factor/totp.html.twig'   # Template used to render the authentication form

    # Trusted devices
#    trusted_device:
#        enabled: true
#        lifetime: 604800               # Lifetime of the trusted device token (604800 = 1 week in seconds) (default to 5184000)
#        extend_lifetime: false         # Automatically extend lifetime of the trusted cookie on re-login
#        cookie_name: trusted_device    # Name of the trusted device cookie
#        cookie_secure: false           # Set the 'Secure' (HTTPS Only) flag on the trusted device cookie
#        cookie_same_site: "lax"        # The same-site option of the cookie, can be "lax" or "strict"
#        #cookie_domain: ".example.com"  # Domain to use when setting the cookie, fallback to the request domain if not set
#        cookie_path: "/"               # Path to use when setting the cookie
